import React from 'react'
import { userObject } from '../../../../context/userContext'
import { Container } from './styles';

const Funding: React.FC<{data: userObject}> = ({data}) => {
    return <Container></Container>
}

export default Funding;